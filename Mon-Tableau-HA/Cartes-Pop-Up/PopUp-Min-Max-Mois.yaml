type: custom:vertical-stack-in-card
card_mod:
  style: |
    ha-card {
      box-shadow: 0 0 0 2px gray;
    }
cards:

  - type: custom:decluttering-card
    template: temperature-min-max
    variables:
      - header: 'Minimum et Maximun mois'
      - entity: 'sensor.minmax_temperature_max_mois'
      - entity2: 'sensor.minmax_temperature_min_mois'
      - temp1: 'sensor.minmax_temperature_max_mois_time'
      - temp2: 'sensor.minmax_temperature_min_mois_time'